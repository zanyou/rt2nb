# Pinned dependencies for the RackTables -> NetBox migration tool.
# Targets Python 3.11+ inside the provided Docker image.
PyMySQL==1.1.1
requests==2.32.3
PyYAML==6.0.2
urllib3==2.2.2

# Test-only (installed via: pip install -r requirements-dev.txt)
